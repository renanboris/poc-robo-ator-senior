{
  "name": "aura-dap-extension-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --testEnvironment jsdom --runInBand"
  },
  "devDependencies": {
    "fast-check": "^3.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
